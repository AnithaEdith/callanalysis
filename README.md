# This is an express JS based API module to invoke the Monkey Learn APIs and converts them to the desired format for displaying various Sentimental Analysis Charts.
# It runs on port 3300

```
npm start
```
